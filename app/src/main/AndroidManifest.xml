<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ActividadescorrutinasKotlin">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".activities.SequentialTasksActivity"
            android:label="@string/activity_1_title"
            android:parentActivityName=".MainActivity" />
        
        <activity
            android:name=".activities.TimerActivity"
            android:label="@string/activity_2_title"
            android:parentActivityName=".MainActivity" />
        
        <activity
            android:name=".activities.ApiSimulationActivity"
            android:label="@string/activity_3_title"
            android:parentActivityName=".MainActivity" />
        
        <activity
            android:name=".activities.WeatherForecastActivity"
            android:label="@string/activity_4_title"
            android:parentActivityName=".MainActivity" />
        
        <activity
            android:name=".activities.FileDownloaderActivity"
            android:label="@string/activity_5_title"
            android:parentActivityName=".MainActivity" />
        
        <activity
            android:name=".activities.NotificationSystemActivity"
            android:label="@string/activity_6_title"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>